Deploy the current project:

1. Run quality checks:
   - Lint/type check if applicable
   - Run tests if they exist
   - Check for security issues (no hardcoded secrets, no SQL injection, etc.)

2. Git status check:
   - Show uncommitted changes
   - Show unpushed commits

3. If all checks pass, ask for confirmation before:
   - Creating a commit with a descriptive message
   - Pushing to remote

4. Report deployment status. Korean 반말.
